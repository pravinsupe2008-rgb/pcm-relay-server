{
  "name": "pcm-relay-server",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "dependencies": {
    "ws": "^8.13.0"
  },
  "scripts": {
    "postinstall": "npm install --omit=dev",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
